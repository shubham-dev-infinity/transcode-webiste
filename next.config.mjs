// /** @type {import('next').NextConfig} */
// const nextConfig = {};

// export default nextConfig;



/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.pexels.com', 'www.narolainfotech.com', 'unsplash.it', 'encrypted-tbn0.gstatic.com'],
    },
};

export default nextConfig;
